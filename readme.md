# Start the app

Use : pip install -r requirements.txt

To run the app (api) : uvicorn app.main:app --reload

In auto-ml\frontend run : python -m streamlit run app.py

In auto-ml\backend run : python -m uvicorn app.main:app --reload
